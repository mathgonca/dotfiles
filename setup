ln -sf $PWD/vimrc  $HOME/.vimrc
  6 ls -l $HOME/.vimrc
#!/bin/bash

ln -sf $PWD/bash.config  $HOME/.bash_aliases
ls -l $HOME/.bash_aliases

ln -sf $PWD/tmux.conf  $HOME/.tmux.conf
ls -l $HOME/.tmux.conf
